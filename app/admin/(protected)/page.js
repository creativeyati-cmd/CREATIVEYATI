import Link from "next/link";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AdminHome() { const supabase = await createSupabaseServerClient(); const [{ count: published = 0 }, { count: drafts = 0 }, { count: categories = 0 }, { count: enquiries = 0 }] = await Promise.all([supabase.from("videos").select("*", { count:"exact", head:true }).eq("status","published"), supabase.from("videos").select("*", { count:"exact", head:true }).eq("status","draft"), supabase.from("categories").select("*", { count:"exact", head:true }), supabase.from("enquiries").select("*", { count:"exact", head:true }).eq("status","new")]); return <><div className="admin-title"><p>OVERVIEW</p><h1>Keep the work moving.</h1></div><div className="stats"><div><span>Published</span><strong>{published}</strong></div><div><span>Drafts</span><strong>{drafts}</strong></div><div><span>Categories</span><strong>{categories}</strong></div><div><span>New enquiries</span><strong>{enquiries}</strong></div></div><div className="admin-actions"><Link className="button" href="/admin/videos/new">Add a video</Link><Link href="/admin/content">Edit website content</Link><Link href="/admin/settings/email">Configure email</Link></div></>; }
