import Link from "next/link";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function Videos() { const supabase = await createSupabaseServerClient(); const { data: videos = [] } = await supabase.from("videos").select("id,title,slug,status,orientation,updated_at").order("updated_at", { ascending:false }); return <><div className="admin-title"><p>VIDEOS</p><h1>Projects</h1><Link className="button" href="/admin/videos/new">Add video</Link></div>{videos.length ? <div className="admin-list">{videos.map((video) => <Link key={video.id} href={`/admin/videos/${video.id}/edit`}><span>{video.title}</span><small>{video.orientation} · {video.status}</small></Link>)}</div> : <p className="empty-state">No videos yet. Add a YouTube project to start your portfolio.</p>}</>; }
