import { getCourseSettings } from "@/lib/data/settings";
import { saveCourseSettings } from "@/app/admin/actions";
export default async function CourseSettingsPage({ searchParams }) { const [settings, query] = await Promise.all([getCourseSettings(), searchParams]); return <><div className="admin-title"><p>COURSES</p><h1>Course settings</h1><p className="admin-lede">Control the restrained course section shown after the homepage introduction.</p></div>{query.saved && <p className="success-note">Course settings saved.</p>}<form className="admin-form" action={saveCourseSettings}><label className="check-label"><input type="checkbox" name="homepageEnabled" defaultChecked={settings.homepageEnabled} />Show featured courses on homepage</label><label>Section heading<input name="homepageHeading" defaultValue={settings.homepageHeading} /></label><label className="form-wide">Introduction<textarea name="homepageCopy" defaultValue={settings.homepageCopy} rows="3" /></label><label>Number of courses<select name="homepageLimit" defaultValue={settings.homepageLimit}><option value="1">1</option><option value="2">2</option><option value="3">3</option></select></label><button className="button">Save course settings</button></form></>; }
