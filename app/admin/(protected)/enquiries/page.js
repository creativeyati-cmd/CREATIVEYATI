import { createSupabaseServerClient } from "@/lib/supabase/server";
import { updateEnquiry } from "@/app/admin/actions";
export default async function Enquiries() { const supabase = await createSupabaseServerClient(); const { data: enquiries = [] } = await supabase.from("enquiries").select("*").order("created_at", { ascending:false }).limit(50); return <><div className="admin-title"><p>ENQUIRIES</p><h1>Inbox</h1></div>{enquiries.length ? <div className="enquiries">{enquiries.map((item) => <form key={item.id} action={updateEnquiry}><input type="hidden" name="id" value={item.id} /><strong>{item.name}</strong><a href={`mailto:${item.email}`}>{item.email}</a><p>{item.message}</p><select name="status" defaultValue={item.status}><option>new</option><option>read</option><option>replied</option><option>archived</option><option>spam</option></select><textarea name="notes" defaultValue={item.internal_notes || ""} placeholder="Internal notes" /><button>Save</button></form>)}</div> : <p className="empty-state">No enquiries yet.</p>}</>; }
