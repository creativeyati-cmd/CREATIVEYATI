import { createSupabaseServiceClient } from "@/lib/supabase/server";
import { formatMoney } from "@/lib/data/courses";
export default async function PaymentsPage() { const service = createSupabaseServiceClient(); const { data: payments = [] } = await service.from("payments").select("*,orders(reference)").order("created_at", { ascending: false }).limit(250); return <><div className="admin-title"><p>COMMERCE</p><h1>Payments</h1></div><div className="admin-table"><div><b>Order</b><b>Gateway reference</b><b>Amount</b><b>Channel</b><b>Status</b></div>{payments.map((payment) => <div key={payment.id}><span>{payment.orders?.reference}</span><span>{payment.gateway_reference}</span><span>{formatMoney(payment.amount_minor, payment.currency)}</span><span>{payment.channel || "—"}</span><span>{payment.status}</span></div>)}</div></>; }
